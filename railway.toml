[build]
buildCommand = "dotnet publish backend/SmeBackend/SmeBackend.csproj -c Release -o out"

[deploy]
startCommand = "dotnet out/SmeBackend.dll --urls http://0.0.0.0:$PORT"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
